Write a program to transpose a matrix (swap its rows and columns) using a nested list comprehension.

SOLUTION:

matrix = [[1, 2, 3],
          [4, 5, 6]]
transpose = [[row[i] for row in matrix] for i in range(len(matrix[0]))]
print(transpose)
